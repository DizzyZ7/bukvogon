[project]
name = "bukvogon-api"
version = "0.1.0"
description = "Backend for the BukvoGon competitive Russian typing game"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "redis>=5,<7",
  "asyncpg>=0.30,<1",
]

[project.optional-dependencies]
dev = [
  "httpx>=0.27,<1",
  "pytest>=8,<9",
  "fakeredis>=2.30,<3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
